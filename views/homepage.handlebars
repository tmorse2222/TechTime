{{#if posts}}
{{#each posts as |post|}}
<div>
    <h2>{{post.title}}</h2>
    <p>{{post.content}}</p>
    <p>{{post.created_at}}</p>
    <p>{{post.user.username}}</p>
</div>
{{/each}}
{{else}}
<h2>No posts right now</h2>
{{/if}}
